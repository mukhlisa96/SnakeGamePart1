## 🐍 Snake Game – Snake Class Module

This module defines the **Snake** class used in a classic Snake game built with Python’s `turtle` graphics library.
It handles the creation, movement, and direction control of the snake.

---

###  Features

* Creates a snake with a predefined starting length
* Moves the snake smoothly across the screen
* Prevents the snake from reversing into itself
* Supports four movement directions (Up, Down, Left, Right)

---

### 📂 Code Overview

* **STARTING_POSITIONS** – Initial positions of the snake segments
* **MOVE_DISTANCE** – Distance the snake moves each step
* **Direction Constants** – Control movement angles
* **Snake Class**

  * `create_snake()` – Builds the initial snake
  * `move()` – Moves the snake forward
  * `up()`, `down()`, `left()`, `right()` – Direction controls with safety checks



